require 'nvim-treesitter.configs'.setup{
  ensure_install = "maintained",
  highlight = {
    enable = true
  },
  indent = {
    enable = true
  },
  incremental_selection = {
    enable = true
  }
}
